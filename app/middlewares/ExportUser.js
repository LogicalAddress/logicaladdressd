var _ = require("underscore");

module.exports = function (app) {
    // app.all('*', function(req, res, next){
    //     app.locals.user = req.session.user ? req.session.user : false
    //     next();
    // });
};